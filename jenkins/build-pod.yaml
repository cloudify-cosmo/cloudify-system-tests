apiVersion: v1
kind: Pod
spec:
  containers:
    - name: py310
      image: python:3.10
      resources:
        requests:
          cpu: 1
      command:
      - cat
      tty: true
      securityContext:
        runAsUser: 0
        privileged: true
  imagePullSecrets:
    - name: dockerhub
  nodeSelector:
    instance-type: spot

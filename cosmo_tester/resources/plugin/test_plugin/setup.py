from setuptools import setup


setup(
    name='test-plugin',
    version='1.0.0',
    packages=[
        'test_plugin',
    ],
    # Please try to avoid adding any dependencies here or the wagon will go
    # from a few KB to many MB.
)

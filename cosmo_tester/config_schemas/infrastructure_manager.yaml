namespace: infrastructure_manager
admin_password:
  description: Password for admin account of infrastructure manager.
  default: admin
address:
  description: IP address or hostname of infrastructure manager.
ca_cert:
  description: The external CA cert of the manager, if SSL is being used for the infrastructure manager.
  default: null
  nullable: true
